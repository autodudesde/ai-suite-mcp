# cat=MCP/enable; type=boolean; label=Enable MCP Server:Enable the MCP endpoint at /aisuite-mcp for Claude Desktop, Claude.ai, ChatGPT, and other MCP clients
enableMcp = 0

# cat=MCP/settings; type=int+; label=MCP Token Lifetime (days):How long OAuth tokens remain valid
mcpTokenLifetimeDays = 30

# cat=MCP/settings; type=string; label=MCP Allowed Origins:Comma-separated list of allowed CORS origins (empty = Development: all allowed, Production: none)
mcpAllowedOrigins =

# cat=MCP/settings; type=string; label=MCP Allowed Client IDs:Comma-separated list of allowed OAuth client IDs (empty = all allowed)
mcpAllowedClientIds =

# cat=MCP/security; type=boolean; label=Allow HTTP (development only):Allow MCP over unencrypted HTTP. NEVER enable in production!
mcpAllowHttp = 0

# cat=MCP/settings; type=options[auto=auto,workspace=workspace,live=live]; label=MCP Write Mode:How MCP tools write data. auto = workspace if available, otherwise live. workspace = always workspace. live = always direct.
mcpWriteMode = auto

# cat=MCP/performance; type=int+; label=MCP Session Timeout (seconds):Drop idle MCP sessions after this many seconds. Lower values free PHP workers and reduce session-store bloat. 0 = SDK default (3600s).
mcpSessionTimeoutSeconds = 1800

# cat=MCP/security; type=string; label=MCP Allowed Redirect URIs:Comma-separated list of allowed external redirect URIs for OAuth (localhost is always allowed)
mcpAllowedRedirectUris =

# cat=MCP/security; type=string; label=MCP Excluded Tables:Comma-separated list of tables that MCP tools must NOT read or write. TYPO3 backend permissions still apply additionally. Use to hide sensitive tables (e.g. be_users, fe_users) even from admins via MCP.
mcpExcludedTables =

# cat=MCP/security; type=string; label=MCP Trusted Proxies:Comma-separated list of trusted reverse-proxy IPs/CIDRs (e.g. 10.0.0.0/8,192.168.0.0/16). When set, audit-log entries resolve the real client IP from X-Forwarded-For instead of the proxy peer IP. Empty = X-Forwarded-For is ignored.
mcpTrustedProxies =

# cat=MCP/logging; type=boolean; label=Verbose INFO log:Write the verbose INFO trace to var/log/aisuite_mcp.log. The WARNING+ alert log (var/log/aisuite_mcp_warnings.log) stays active either way. Disable in production once monitoring is mature to reduce I/O and PII surface.
mcpLogVerbose = 1

# cat=MCP/logging; type=string; label=MCP Log Redaction Patterns:Comma-separated additional regex patterns (without delimiters) whose matches are replaced with [REDACTED] in MCP log entries. Built-in defaults (Bearer tokens, 64-char hex hashes, email addresses) are always applied — these patterns are added on top.
mcpLogRedactionPatterns =

# cat=MCP/media; type=string; label=Media Upload Default Folder:FAL combined identifier for the uploadMedia tool when no targetFolder is given (e.g. 1:/user_upload/).
mcpMediaDefaultFolder = 1:/user_upload/

# cat=MCP/media; type=int+; label=Media Upload Max Size (MB):Maximum size for a single uploaded/downloaded media file. Applies to URL downloads and base64 uploads. 0 = no explicit limit (PHP/storage limits still apply).
mcpMediaMaxSizeMb = 50

# cat=MCP/media; type=string; label=Media Allowed Extensions:Comma-separated lowercase file extensions the uploadMedia tool may store (URL download / base64). SVG is intentionally excluded by default (XSS). Online-media links (YouTube/Vimeo) are governed by TYPO3's online media helpers, not this list.
mcpMediaAllowedExtensions = jpg,jpeg,png,gif,webp,avif,mp4,webm,ogg

# cat=MCP/media; type=boolean; label=Allow Media URL Fetch:Allow the uploadMedia tool to download files from remote http(s) URLs. When disabled, only base64 upload and online-media links (YouTube/Vimeo) are accepted. URL fetching is SSRF-guarded (private/loopback/link-local targets are always blocked), but you can disable it entirely here.
mcpMediaAllowUrlFetch = 1

# cat=MCP/media; type=string; label=Media URL Host Denylist:Comma-separated additional hostnames or IP/CIDR ranges that uploadMedia must never fetch from (on top of the always-on private/loopback/link-local block). Example: metadata.internal,10.0.0.0/8
mcpMediaHostDenylist =
